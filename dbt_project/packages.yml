# Use `dbt deps` in order to install below packages
# Pin dbt_utils to a version compatible with dbt-core==1.9.1.
# 1.3.1 is reported compatible by the registry with dbt-core 1.9.x in this environment.
# If you later upgrade/downgrade dbt-core, revisit this pin.
packages:
  - package: dbt-labs/dbt_utils
    version: "1.3.1"